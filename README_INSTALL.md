# Установка Citadex

Основная инструкция находится в [README.md](README.md). Для релизной установки используйте:

```bash
python -m pip install citadex
citadex --help
```

Для локальной разработки клонируйте репозиторий и установите `python -m pip install -e ".[dev]"` в виртуальном окружении.
